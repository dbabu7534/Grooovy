class CharCount {
    static void main(String[] args){
        def str = "Dineshbabu"
        int length = 0

        for(i in 1..str.length()){
            length++
        }

        println(length)


    }
}
